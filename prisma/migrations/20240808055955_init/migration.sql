BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Form] ADD CONSTRAINT [Form_age_df] DEFAULT 0 FOR [age];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
